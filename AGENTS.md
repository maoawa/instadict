# Apple platform UI

Prefer Apple's native controls, navigation, search, presentation, and layout APIs whenever an appropriate one exists. Let the system provide platform styling, including Liquid Glass, focus, dictation, and inactive-state dimming. Use custom components only for behavior the native APIs do not provide, and retain native fallbacks for supported older OS versions.